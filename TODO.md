# TODO

## Prepare PDB — 3D viewer / altLoc

- [ ] **1. Licorice не отображается.** В фокусном altLoc-просмотре (`build_altloc_html`)
  конформеры должны рисоваться как licorice (толстый stick), но сейчас ничего не видно.
  Разобраться почему — вероятно селектор `altloc` в 3Dmol AtomSpec не совпадает
  (проверить имя поля: `altLoc` vs `altloc`, регистр кода конформера, тип `resi`).

- [ ] **2. Переходы внутри одного 3D-окна, без перезапуска процесса.**
  Сейчас каждый "View 3D" / "Show in 3D" поднимает новый процесс pywebview и новое окно.
  Нужно: одно уже открытое окно, а действия (показать всё / выбрать какие группы
  оставить-убрать / сфокусироваться на конкретной alt-позиции) меняют сцену
  внутри него (через 3Dmol JS + мост pywebview js_api Python→JS).
  - Show in 3D → показать всю структуру, переключение групп меняет стиль на месте
  - View конкретной alt-позиции → камера и подсветка перестраиваются в том же окне

- [ ] **3. Показывать все alt-позиции одновременно.**
  Все конформеры остатка должны быть видны сразу (каждый своим цветом),
  а не по одному. Связано с пунктом 1 — проверить после починки licorice.
